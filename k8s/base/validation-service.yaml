apiVersion: v1
kind: Service
metadata:
  name: transaction-validation
spec:
  selector:
    app: transaction-validation
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP