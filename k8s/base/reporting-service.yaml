apiVersion: v1
kind: Service
metadata:
  name: transaction-reporting
spec:
  selector:
    app: transaction-reporting
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP